<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Diseños Alma | Haz tu diseño único" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "devise_custom", media: "all" %>


    <!-- Enlace al archivo CSS de Bootstrap desde el CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Enlace a la fuente de Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_importmap_tags %>

    <%# Control de temas (Modo oscuro y modo claro) %>
    <style>
      html[data-theme="dark"],
      html[data-theme="light"] {
        color: inherit;
        background-color: inherit;
      }

      body[data-theme="dark"] {
        background-color: #121212;
        color: white;
      }
      body[data-theme="light"] {
        background-color: white;
        color: black;
      }

      body{
        transition: background-color 0.3s, color 0.3s;
      }

      /* Estilos para el carrito lateral */
      .side-cart {
        position: fixed;
        top: 0;
        right: 0;
        width: 360px;
        max-width: 100%;
        height: 100%;
        background: white;
        box-shadow: -2px 0 10px rgba(0,0,0,0.3);
        transform: translateX(100%);
        transition: transform 0.3s ease-in-out;
        z-index: 1055;
        display: flex;
        flex-direction: column;
      }

      .side-cart.open {
        transform: translateX(0);
      }

      .side-cart.hidden {
        display: none;
      }

      .cart-button {
        display: block;
        width: 100%;
        text-align: center;
        background-color: black;
        color: white;
        padding: 12px;
        text-transform: uppercase;
        font-weight: bold;
        border-radius: 25px;
        text-decoration: none;
      }
    </style>
    <script>
      (function() {
        try {
          const theme = localStorage.getItem('theme') || 'light';
          document.body.setAttribute('data-theme', theme);
        } catch(e) {
          document.body.setAttribute('data-theme', 'light');
        }
      })();
    </script>
  </head>

  <body>
    <!-- Barra de navegación -->
    <% unless controller_path.starts_with?("admin/") %>
      <%= render 'shared/navbar' %>
    <% end %>
    <%= yield %>
    <!-- Panel lateral del carrito -->
    <div id="side-cart" class="side-cart hidden">
      <div class="side-cart-header d-flex justify-content-between align-items-center p-3 border-bottom">
        <h5 class="mb-0"><strong>CART</strong> (<span id="cart-count"><%= current_cart.cart_items.sum(:quantity) rescue 0 %></span> items)</h5>
        <button id="cart-close" class="btn-close" aria-label="Close"></button>
      </div>

      <div class="side-cart-content p-3">
        <%= render partial: 'shared/side_cart_content' %>

      </div>

      <div class="side-cart-footer p-3 border-top">
        <%= link_to "SHOP ALL", categories_path(locale: I18n.locale), class: "cart-button mb-2" %>
        <%= link_to "HOME", root_path(locale: I18n.locale), class: "cart-button mb-2" %>
        <%= link_to "SHOP", categories_path(locale: I18n.locale), class: "cart-button" %>
      </div>
    </div>


    <script>
      document.addEventListener("turbo:load", () => {
        const cartToggle = document.getElementById("cart-toggle");
        const cartToggleXL = document.getElementById("cart-toggle-xl");
        const cartClose = document.getElementById("cart-close");
        const sideCart = document.getElementById("side-cart");

        if (cartToggle) {
          cartToggle.addEventListener("click", () => {
            sideCart.classList.add("open");
            sideCart.classList.remove("hidden");
          });
        }

        if (cartToggleXL) {
          cartToggleXL.addEventListener("click", () => {
            sideCart.classList.add("open");
            sideCart.classList.remove("hidden");
          });
        }

        if (cartClose) {
          cartClose.addEventListener("click", () => {
            sideCart.classList.remove("open");
            setTimeout(() => sideCart.classList.add("hidden"), 300);
          });
        }
      }); 
</script>

  </body>
</html>